---
// Test page for authentication functionality
---

<html lang="nl">
  <head>
    <style is:global>
      @import "../styles/global.css";
    </style>
    <meta charset="UTF-8" />
    <title>Authentication Test - Kitebuddy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body class="bg-amber-50 font-sans text-gray-800 min-h-screen">
    <header class="bg-cyan-700 text-white text-center p-3 sm:p-4">
      <h1 class="text-lg sm:text-xl md:text-2xl lg:text-3xl font-semibold leading-tight">
        🔧 Authentication Test
      </h1>
      <a href="/" class="text-xs sm:text-sm text-cyan-200 hover:text-white mt-2 inline-block min-h-[44px] min-w-[44px] flex items-center justify-center">
        ← Terug naar Kitebuddy
      </a>
    </header>

    <main class="w-full max-w-4xl mx-auto px-3 sm:px-4 lg:px-6 py-3 sm:py-4 lg:py-6">
      
      <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
        <h2 class="text-xl font-bold text-gray-800 mb-4">🔧 Authentication Test Page</h2>
        
        <div class="space-y-4">
          <div class="p-4 bg-blue-50 border border-blue-200 rounded-md">
            <h3 class="font-semibold text-blue-800 mb-2">📋 Test Instructions:</h3>
            <ol class="list-decimal list-inside text-blue-700 space-y-1">
              <li>Make sure your .env file has real Supabase credentials</li>
              <li>Configure Supabase for localhost (see instructions below)</li>
              <li>Click the "Sign up" button to test registration</li>
              <li>Check your email for verification (if configured)</li>
              <li>Try logging in with your credentials</li>
            </ol>
          </div>

          <div class="p-4 bg-yellow-50 border border-yellow-200 rounded-md">
            <h3 class="font-semibold text-yellow-800 mb-2">⚠️ Localhost Configuration:</h3>
            <p class="text-yellow-700 mb-2">In your Supabase dashboard, add these URLs:</p>
            <ul class="list-disc list-inside text-yellow-700 space-y-1">
              <li><strong>Site URL:</strong> http://localhost:4321</li>
              <li><strong>Redirect URLs:</strong> http://localhost:4321/auth/callback</li>
            </ul>
          </div>

          <div class="p-4 bg-green-50 border border-green-200 rounded-md">
            <h3 class="font-semibold text-green-800 mb-2">✅ Alternative: Use ngrok</h3>
            <p class="text-green-700 mb-2">For a public URL that works with email verification:</p>
            <ol class="list-decimal list-inside text-green-700 space-y-1">
              <li>Install ngrok: <code class="bg-green-100 px-1 rounded">https://ngrok.com/download</code></li>
              <li>Run: <code class="bg-green-100 px-1 rounded">ngrok http 4321</code></li>
              <li>Use the public URL in Supabase settings</li>
            </ol>
          </div>
        </div>
      </div>

      <!-- Include the Header component for authentication testing -->
      <div id="auth-test-container">
        <script>
          // This will be replaced by the Header component
          document.getElementById('auth-test-container').innerHTML = `
            <div class="bg-white rounded-lg shadow-lg p-6">
              <h3 class="text-lg font-semibold text-gray-800 mb-4">🔐 Authentication Test</h3>
              <p class="text-gray-600 mb-4">The authentication system is loaded in the header above. Use the Login/Sign up buttons to test.</p>
              
              <div class="space-y-2">
                <div class="flex items-center space-x-2">
                  <span class="w-3 h-3 bg-gray-300 rounded-full"></span>
                  <span class="text-sm text-gray-600">Authentication system status: Loading...</span>
                </div>
              </div>
            </div>
          `;
        </script>
      </div>

      <div class="bg-white rounded-lg shadow-lg p-6 mt-6">
        <h3 class="text-lg font-semibold text-gray-800 mb-4">🔍 Debug Information</h3>
        
        <div class="space-y-3">
          <div class="p-3 bg-gray-50 rounded-md">
            <strong>Current URL:</strong> <span id="current-url"></span>
          </div>
          <div class="p-3 bg-gray-50 rounded-md">
            <strong>Environment:</strong> <span id="environment"></span>
          </div>
          <div class="p-3 bg-gray-50 rounded-md">
            <strong>Supabase Config:</strong> <span id="supabase-config"></span>
          </div>
        </div>
      </div>

    </main>

    <script>
      // Debug information
      document.getElementById('current-url').textContent = window.location.href;
      document.getElementById('environment').textContent = window.location.hostname === 'localhost' ? 'Development (localhost)' : 'Production';
      
      // Check Supabase configuration
      const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL;
      const supabaseKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;
      
      if (supabaseUrl && supabaseKey && !supabaseUrl.includes('your-project-id')) {
        document.getElementById('supabase-config').textContent = '✅ Configured';
      } else {
        document.getElementById('supabase-config').textContent = '❌ Not configured - check .env file';
      }
    </script>
  </body>
</html>
